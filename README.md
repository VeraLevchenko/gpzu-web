# 🏗️ ГПЗУ Web Application

Веб-приложение для автоматизации работы с градостроительными планами земельных участков (ГПЗУ).

## 📋 Возможности

- ✅ Парсинг заявлений из DOCX файлов
- ✅ Автоматическое создание задач в Kaiten
- ✅ Расчёт сроков оказания услуг (+14 рабочих дней)
- ✅ Веб-интерфейс для удобной работы

## 🛠️ Технологии

**Backend:**
- FastAPI (Python 3.11+)
- python-docx для парсинга документов
- aiohttp для интеграции с Kaiten API

**Frontend:**
- React 18
- Axios для HTTP запросов
- Современный UI/UX

## 📦 Установка

### Требования

- Python 3.11+
- Node.js 16+
- Git

### Backend
```bash
cd backend

# Создаём виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Настраиваем .env
cp .env.example .env
nano .env  # Вставьте ваши токены
```

### Frontend
```bash
cd frontend

# Устанавливаем зависимости
npm install

# Для разработки
npm start

# Для production сборки
npm run build
```

## ⚙️ Конфигурация

Создайте файл `backend/.env`:
```ini
# Kaiten API
KAITEN_API_TOKEN=ваш_токен
KAITEN_DOMAIN=isogd2019.kaiten.ru

# Kaiten Board Settings
KAITEN_SPACE_ID=696620
KAITEN_BOARD_ID=1578215
KAITEN_COLUMN_ID=5474955
KAITEN_LANE_ID=1948738

# Кастомные поля Kaiten
KAITEN_FIELD_CADNUM=id_238069
KAITEN_FIELD_SUBMIT_METHOD=id_270924
KAITEN_SUBMIT_METHOD_EPGU=93413
KAITEN_FIELD_INCOMING_DATE=id_228500
KAITEN_FIELD_UNKNOWN=id_270916

# Безопасность
SECRET_KEY=измените-этот-ключ-в-продакшене

# CORS
FRONTEND_URL=http://localhost:3000
```

## 🚀 Запуск

### Режим разработки

**Терминал 1 - Backend:**
```bash
cd backend
source venv/bin/activate
python main.py
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm start
```

Откройте http://localhost:3000

### Production режим
```bash
cd frontend
npm run build

cd ../backend
source venv/bin/activate
python main.py
```

Откройте http://localhost:8000

## 📚 API Endpoints

### Парсинг заявления
```http
POST /api/gp/kaiten/parse-application
Content-Type: multipart/form-data

file: application.docx
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "number": "6422028095",
    "date": "2025-11-15",
    "applicant": "Иванов Иван Иванович",
    "cadnum": "42:30:0102050255",
    "purpose": "ИЖС",
    "service_date": "2025-12-05"
  }
}
```

### Создание задачи в Kaiten
```http
POST /api/gp/kaiten/create-task
Content-Type: application/json

{
  "application": {
    "number": "6422028095",
    "date": "2025-11-15",
    "applicant": "Иванов Иван Иванович",
    "cadnum": "42:30:0102050255",
    "service_date": "2025-12-05"
  }
}
```

**Ответ:**
```json
{
  "success": true,
  "card_id": 58341404,
  "card_url": "https://isogd2019.kaiten.ru/space/696620/boards/card/58341404"
}
```

## 🔧 Структура проекта
```
gpzu-web/
├── backend/
│   ├── api/              # API endpoints
│   ├── core/             # Конфигурация
│   ├── parsers/          # Парсеры документов
│   ├── utils/            # Вспомогательные функции
│   ├── main.py           # Точка входа
│   └── requirements.txt
│
├── frontend/
│   ├── public/
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── services/     # API клиенты
│   │   └── App.js
│   └── package.json
│
└── README.md
```

## 📝 Логика работы

1. Пользователь загружает заявление (DOCX)
2. Backend парсит документ и извлекает данные
3. Рассчитывается срок оказания услуги (+14 рабочих дней)
4. Создаётся карточка в Kaiten со всеми данными:
   - Название: "{номер} {заявитель}"
   - Кадастровый номер
   - Срок выполнения
   - Способ подачи: ЕПГУ
5. Пользователь получает ссылку на созданную карточку

## 🤝 Вклад в проект

Если вы хотите внести свой вклад:

1. Форкните репозиторий
2. Создайте ветку для ваших изменений
3. Внесите изменения и сделайте коммит
4. Отправьте Pull Request

## 📄 Лицензия

MIT License

## 👤 Автор

VeraLevchenko

---

**⭐ Поставьте звезду, если проект оказался полезным!**
