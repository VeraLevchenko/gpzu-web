# Plan: Добавление "И.о.нач.отдела" и "Стрекалина Н.В." в шаблон А3

## Context
В шаблоне рабочего набора А3 (`map1_a3_landscape.wor.txt`) в блоке подписей сейчас две строки:
- "Зам. нач. отдела" / "Бейгель А.Н."
- "Гл.специалист" / "{{SPECIALIST}}"

Нужно добавить третью строку ниже "Гл.специалист": "И.о.нач.отдела" / "Стрекалина Н.В."

## Анализ структуры таблицы подписей

Горизонтальные линии таблицы (из файла):
- `10.0813` — нижняя граница заголовка
- `10.2833` — разделитель строк
- `10.4896` — разделитель строк (после "Гл.специалист")
- `10.6931` — следующая линия (слот уже существует — пустой!)
- `10.8965`, `11.0972`, `11.3028` — далее

Слот между `10.4896` и `10.6931` уже существует и пуст — туда и добавляем новую строку.

## Изменения

**Файл:** `backend/templates/wor/layouts/map1_a3_landscape.wor.txt`

После блока `{{SPECIALIST}}` / `Гл.специалист` (строки 648–656) добавить два новых текстовых блока:

```
  Create Text
    "И.о.нач.отдела"
    (8.9549,10.5417) (9.65,10.6737)
    Font ("Times New Roman CYR",0,8,0)

  Create Text
    "Стрекалина Н.В."
    (9.8174,10.5417) (10.4104,10.6737)
    Font ("Times New Roman CYR",0,8,0)
```

Y-координаты рассчитаны по той же логике отступов, что и существующие строки (отступ 0.052 от верхней линии слота).

## Место вставки
Сразу после строки 666 (`Font ("Times New Roman CYR",0,8,0)` у "Бейгель А.Н.") — НЕТ.

Правильное место: **после строки 656** (после блока "Гл.специалист"), так как новая строка должна быть ПОД "Гл.специалист".

Точнее: вставить после всего блока `{{SPECIALIST}}` + `Гл.специалист` (строки 648–656), перед `Зам. нач. отдела`.

Фактически: добавить в конец блока подписей, после строки 666 (блок "Бейгель А.Н.").

## Итоговый порядок строк в таблице (снизу вверх по Y):
1. "Зам. нач. отдела" / "Бейгель А.Н." — Y ~10.11–10.24
2. "Гл.специалист" / "{{SPECIALIST}}" — Y ~10.33–10.47
3. "И.о.нач.отдела" / "Стрекалина Н.В." — Y ~10.54–10.67 (новые)

## Проверка
1. Открыть рабочий набор через модуль ГПЗУ → генерация рабочего набора
2. Проверить, что в блоке подписей появились "И.о.нач.отдела" и "Стрекалина Н.В." на одном уровне
3. Убедиться, что они вписываются в существующий пустой слот таблицы (между линиями 10.4896 и 10.6931)
